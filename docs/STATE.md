# Speedrun — START HERE (current state & handoff)

**If you're a fresh chat/agent: read this, then `docs/PROGRESS.md` (done/left tracker) → `docs/FUTURE-PLANS.md` (backlog) → `docs/DECISIONS.md` → `docs/PRD.md` → `docs/BUILD-WORKFLOW.md` → `brainlift/BrainLift.md`. Run steps: `docs/RUN-MVP.md`. Context hygiene: `docs/CONTEXT-ENGINEERING.md`. `AGENTS.md` (root) auto-loads the invariants. Cross-agent channel: `.claude/cursor-review.md` (Cursor↔Claude; read its top "## Pending").**

## 🔴 LIVE HANDOFF (updated 2026-07-05 — Sunday final-submission evidence merged)
**Sunday update (2026-07-05):** all 3 Sunday tasks DONE + merged — **(A)** memory calibration (Brier 0.0569 / log-loss 0.2177 / ECE 0.0042, labeled-simulated) → anki `main` **`419726d93`**; **(B)** performance eval (simulated predictive + hermetic auto-grader 50/50 real gold) → umbrella `main` **`595ed75`**; **(C)** signed **arm64-v8a release APK** (50.2 MB, apksigner-verified) → anki-android `main` **`073c0f4459`** + AAB `main` **`b69ebcc`**. Numbers written into `docs/RESULTS.md` (§9.1/§9.2/Part 4). **Remaining = human-only:** recordings (phone install/run on a clean device, live two-way+offline sync, 3–5 min demo video) + BrainLift PDF re-export.
**Friday pins (historical):** anki `main` **`8cd09ec51`** (Friday scope + LS1/2/3 + ablation + all-7-sweep-fixes + MCQ auto-grade + 4 visuals + median-tick fix + AI-generate button + **installer bundles+auto-imports seed deck**) · anki-android **`6845e4e70a`** · Anki-Android-Backend **`5e02a2b`** (rsdroid re-pinned → anki `7bcf9ee10`, engine-identical; AAR rebuilt + APK ready) · umbrella **`b64a808`** (+ AI `/generate_batch` service).
**In-flight (Claude, building SEQUENTIALLY per disk constraint; priority order):**
1. ✅ **AI-generate button (8pm demo headliner) — DONE + MERGED (2026-07-03 17:35).** `feat/ai-generate-button` @ `1baef95eb` → **anki `main` `1baef95eb`** (Qt handler + Svelte button on The Map + mediasrv map-reload fix); `feat/ai-generate-service` @ `a2d89a4` → **umbrella `main` `b64a808`** (`/generate_batch`, verified-only). Feature: tap a covered leaf node on The Map → "⚡ Generate 5 practice problems" → Qt HTTP→external service→import ONLY verified as `Speedrun::Problem` tagged `ai-generated` w/ Source; OFF-by-default, desktop-first.
2. ✅ **AAR rebuild — DONE + MERGED (2026-07-03 19:00).** `build/aar-repin-latest` @ `5e02a2b30` FF'd → **Anki-Android-Backend `main` `5e02a2b`** (pushed). rsdroid `anki` submodule re-pinned `8ca3112d7`→`7bcf9ee10`; AAR (21 MB, x86_64) bundled-UI verified (getCalibration + The Map + 3 scores + calibration + mini-mock); **APK ready** at `repos/anki-android/AnkiDroid/build/outputs/apk/play/debug/AnkiDroid-play-x86_64-debug.apk`. Unblocks emulator gate + sync demo.
3. ✅ **installer-bundles-deck — DONE (code + MSI) (2026-07-03 19:25).** Code merged → anki `main` `8cd09ec51` (`aqt.rs` bundles `gre_math_seed.apkg` 196 KB; `main.py`+`speedrun_logic.py` = idempotent, config-gated `speedrunSeedImportEnabled`, post-sync, non-safeMode first-run auto-import; skips if the exam deck exists; 185 lines of aqt tests green; both bug-hunts clean). **RELEASE MSI built + fresh-profile auto-import proven** (headless Collection against the packaged app) → **`repos/anki/out/installer/dist/anki-26.05-win-x64.msi` (193.7 MB, valid, full Friday UI + bundled deck)** — copied to the canonical path (the build worktree may be pruned). Two P2 UX-diagnostic notes logged post-demo (non-blocking).
**ALL THREE build tasks (AI button + AAR + installer) are DONE + merged.** Every code repo has ZERO unmerged branches (verified `git branch --no-merged main` empty on all 3). Remaining = human record/eval only.
**Cursor next actions:** demo-critical builds (AI button + AAR) are DONE + merged. Watch `.claude/cursor-review.md` for the installer gate (post-demo) → merge. Hand David the demo runbook below.
**8pm DEMO RUNBOOK (David; needs AI button + AAR merged first):**
- **Populate data** (so visuals/scores light up): install/launch desktop → import `repos/anki/speedrun/out/gre_math_seed.apkg` (or it's pre-loaded once installer-deck lands) → START RUN + grade ~20 cards on a topic → run a mini-mock → place a few Sure/Think/Guess confidence bets.
- **AI button:** start the service — in `services/speedrun-ai/`: set `OPENAI_API_KEY` (in `.env`) + `SPEEDRUN_AI_ENABLED=1`, run `uv run uvicorn app:app --port 8000` (confirm health) → desktop The Map → tap Calculus → "⚡ Generate 5" → verified, cited problems import.
- **Sync (after AAR):** launch `anki-sync-server` (`docs/SYNC-SELFHOST.md`) → point desktop + emulator at it → two-way + offline-reconnect run.
- **Then record** the demo video (`docs/DEMO-VIDEO-SCRIPT.md`, already rewritten for these beats).
**Still pending (Sunday):** eval RUNS (calibration reliability, held-out performance accuracy, score-mapping writeup), robustness (crash×20/offline/`make bench`), signed APK, BrainLift final pass.

## What this is
Speedrun: an honest GRE **Mathematics Subject Test** study app built on **Anki** (desktop + Android sharing one Rust engine). Owner: David Ordonez (GitHub `spinkicks`). License AGPL-3.0-or-later, credit Anki.

## Status (as of 2026-07-03, Fri PM — FULL Friday scope + LS1/2/3 + ablation + AI service + all 7 sweep fixes + interactive MCQ auto-grade ALL MERGED; 4 interactive visuals live-verified + Claude-reviewed, merging to anki `main`; only human demo/eval remains)

**TL;DR (2026-07-03 PM):** Everything on the Friday plan plus the 3 greenlit learning-science additions, the §8 ablation harness, the P0/P1/P2/P3 fix batches, the OFF-by-default AI service, **all 7 adversarial-sweep bug fixes** (incl. #3 RAG grounding via a semantic-embedding gate + fail-closed syllabus scoping, and #4 leakage), and the **interactive MCQ auto-grade** (backend key-checked correctness) are **built, gate-reviewed, and merged to `main`.** **NEW today — 4 interactive pure-SVG visuals** on `feat/speedrun-visuals` (live-verified + Claude-reviewed, merged to anki `main` `348db0c6c`): **THE MAP** (`/speedrun-map`, an interactive prerequisite-graph, tap→blast-radius — realizes **BrainLift Flagship #7**, the signature not-Anki visual), a **calibration reliability diagram** + **memory→performance gap slope chart** (Memory), and a **readiness gauge** (Home, 200–990 + conformal band); both platforms, honest abstains, 11/11 unit tests. **Per-repo `main`:** anki **`7bcf9ee10`** · anki-android **`6845e4e70a`** · Anki-Android-Backend **`70b8eaf`** · umbrella `spinkicks/speedrun` **`5f0be27`**. **Remaining = human/Sunday only:** Android emulator visual gate, live desktop↔Android sync-demo recording, demo video, Sunday eval RUNS (calibration reliability + Brier/log-loss, performance accuracy on held-out, score-mapping writeup), robustness (crash×20 / offline / `make bench`), signed APK, final BrainLift pass. **NEW in-progress (Claude):** installer bundles the seed deck + auto-imports it on first launch (grader convenience).

## Status (historical — as of 2026-07-02, Thu AM — Wed MVP shipped+recorded; Friday plan approved & executing + branding slice greenlit)
- ✅ Planning artifacts complete: BrainLift (9 SPOVs), PRD, ARCHITECTURE, BUILD-WORKFLOW, DECISIONS. Toolchain + Serena MCP working (see git history of this file for setup details).
- ✅ **Walking skeleton** (Mon–Tue): forked Anki builds/runs; `SpeedrunService.GetCoverage` read-only RPC via TDD; one-engine-two-apps proven on the `Pixel_10` x86_64 emulator (instrumentation test, backendVersion 26.05 both platforms).
- ✅ **Wednesday MVP + Wednesday-Plus — ALL MERGED TO `main` on all 3 forks (2026-07-01):**
  - Engine: 5 RPCs on `SpeedrunService`, proto FROZEN — `GetCoverage`, `GetTopicMastery` (Wilson 95% + abstain), `GetExamProfile` (synced config), **`ReorderNewByPointsAtStake`** (mutating via `transact(Op::SortCards)`, undo-safe, ablation Full/FeatureOff/Plain), `GetPerformanceReadiness` (always-abstain scaffolding).
  - Content: `gre_math.json` exam DAG + 35-card seed deck (tag↔topic alignment audit-verified) + deterministic scraper. NO AI anywhere.
  - Installer: network-independent (Briefcase templates vendored; SyncSubmodule dropped).
  - Dashboard: shared Svelte Memory page on both platforms (desktop Qt dialog + Android PageFragment; AAR auto-bundles sveltekit assets).
  - Sync: `anki-sync-server` in-fork + §7b conflict test (revlog union + latest-wins, honest caveat documented) → `docs/SYNC-SELFHOST.md`.
- **Per-repo `main` pins (single source of truth):** anki **`1fed9e109`** · Anki-Android-Backend **`d4086e0`** (contains `299bb44` rsdroid `anki`-submodule pin → **`a0ead51c9`**; note the pin is a wed-plus-branch commit, ancestor-equivalent content to main tip) · anki-android **`a56dda6cfb`** · umbrella `spinkicks/speedrun` `main` = latest. All FF, no merge commits; `feat/speedrun-wed-plus` branches kept as backup. Upstream tracking on `main` fixed on all forks.

## Immediate next step (2026-07-03 PM) — everything buildable is MERGED (all 7 sweep fixes + MCQ auto-grade); visuals merging to anki `main`; only human demo/eval remains
1. **Merge `feat/speedrun-visuals` → anki `main`** (live-verified + Claude-reviewed; SHA finalizes at merge): THE MAP (`/speedrun-map`, tap→blast-radius) + calibration reliability diagram + memory→performance gap slope chart + readiness gauge; pure-SVG, both platforms, honest abstains, 11/11 unit tests.
2. **Installer bundles seed deck (in progress, Claude):** bundle the seed deck + auto-import on first launch so a grader sees live data immediately — convenience only, not on any build critical path.
3. **David — demo/record (the only build-independent work):** Android emulator visual gate (3 scores + the 4 visuals + calibration buttons + faded reveal + Manrope) → live desktop↔Android self-hosted sync demo (`docs/SYNC-SELFHOST.md`) → MVP demo video (`docs/DEMO-VIDEO-SCRIPT.md`).
4. **Sunday workstream** (`docs/FUTURE-PLANS.md`): eval RUNS (calibration reliability + Brier/log-loss; performance accuracy on held-out; score-mapping writeup), robustness (crash×20 / offline / `make bench` p50/p95 on 50k deck), signed APK, final BrainLift pass. (Ablation harness + gold set + Problem bank + all 7 sweep fixes are already shipped.)

### History (all ✅ MERGED — kept for the audit trail)
- **Wed MVP (2026-07-01):** one engine two apps; 5 RPCs; honest Memory (Wilson+abstain); Speedrun Home + Memory dashboard; START RUN; self-hosted sync + §7b test; offline installer (release MSI, 27/27). Clean-machine install recorded (`CleanTestInstall.mp4`).
- **Fri scope (2026-07-02→03):** Performance + Readiness scores (in-engine, honest, abstaining); due-card weakness×topic interleave; `Speedrun::Problem` bank (64, double-SymPy-verified) + timed mini-mock; AI/RAG service (OFF by default, SymPy + gold-gate); Manrope/#F4F7FA identity; Phase 6 AAR rebuild + APK.
- **LS additions (2026-07-02):** LS1 calibration, LS2 worked-examples-faded, LS3 honesty-copy.
- **Quality (2026-07-02→03):** §8 ablation harness (pre-registered M1/M2/M3); P0 honesty + P1 AI-safety + P2 mini-mock + P3 nit batches.
- **Interactive layer (2026-07-03 PM):** interactive **MCQ auto-grade** MERGED (backend key-checked → Performance now objectively graded, retires the "self-reported" caveat on desktop; Android capture deferred); **all 7 adversarial-sweep bugs fixed+merged** (incl. #3 RAG grounding via semantic-embedding gate + fail-closed syllabus, #4 leakage); **4 interactive pure-SVG visuals** (THE MAP blast-radius / calibration reliability diagram / memory→performance gap slope / readiness gauge) live-verified + Claude-reviewed, merging to anki `main`.
- **David inputs — ALL RESOLVED:** wordmark = Manrope; accent = `#F4F7FA`; `PROBLEM_MODEL_ID=2047815909`; OpenAI key in `services/speedrun-ai/.env`; gold set delivered (`eval/holdout/gre_math_gold.jsonl`, 50, triple-verified, leakage-cleared).

## Per-repo `main` pins (updated 2026-07-03 PM, post sweep-fix + MCQ-autograde merges)
- **anki `main` = `348db0c6c`** — Phases 0–6 + P0/P1/P2/P3 fixes + LS1 calibration + LS2 worked-examples + LS3 honesty-copy + §8 ablation harness + Manrope/white identity + **all 7 sweep fixes** (band-abstain, calibration-capture, RAG grounding gate, leakage) + **interactive MCQ auto-grade** (backend key-checked Performance). The 4 interactive visuals (`feat/speedrun-visuals`) are MERGED on top (clean 3-way; this SHA).
- **anki-android `main` = `6845e4e70a`** — P0 Android (real mini-mock + bridge parity) + README banner + P0 #2 `getCalibration` exposure.
- **Anki-Android-Backend `main` = `70b8eaf`** — Phase 6 rsdroid re-pin to the P0-complete engine + AAR rebuilt (21 MB, x86_64, UI-in-AAR verified) + README banner. AAR-rebuild safety restored (`getCalibration` now on anki-android `main`).
- **umbrella `spinkicks/speedrun` `main` = `e36d765`** — AI service (`services/speedrun-ai/`, OFF by default, curated 64-problem bank ships in-app) with all sweep-fix consolidation; all gate-approval docs + this file.
- **Branch status:** every **feature** branch in all 4 repos is an ancestor of `main` (fully merged), including **all 7 sweep-fix branches** (#3 RAG grounding + #4 leakage are now merged, no longer held). Backup `feat/*` branches retained. **EXCEPTION — intentionally NOT yet merged:** `feat/speedrun-visuals` (anki) — the 4 interactive visuals, live-verified + Claude-reviewed, merging to anki `main` (SHA finalizes at merge).
- **Current fork tips (2026-07-03 PM):** anki **`348db0c6c`**, anki-android **`6845e4e70a`**, Anki-Android-Backend **`70b8eaf`**, umbrella **`e36d765`**. **Sweep-fix status:** #1–#7 ALL MERGED (#3 RAG grounding fixed via semantic-embedding gate + fail-closed syllabus scoping; #4 leakage merged). **Visuals:** `feat/speedrun-visuals` MERGED → anki `main` `348db0c6c`. **Pending merge:** none (Android AAR rebuild for the emulator gate is the next build step).

## CRITICAL PATH — ✅ COMPLETE (2026-07-03). All build phases done + merged.
The entire Gate 2 → Phase 3 → Phase 4 (AI) → Phase 5 → Phase 6 chain plus the Manrope branding, LS1/2/3, §8 ablation, all 7 sweep bug fixes, and the interactive MCQ auto-grade are **built, gate-reviewed, and merged to `main`.** What remains is not on any build critical path — it's the human demo/eval work, the `feat/speedrun-visuals` merge (the 4 interactive visuals, already live-verified + Claude-reviewed), and the in-progress installer seed-deck bundling (see "Immediate next step" above).
- **✅ Gate 2** (proto + honest Performance/Readiness), **✅ Phase 3** (Problem bank + mini-mock), **✅ Phase 4** (AI service, kill-switched), **✅ Phase 5** (3 scores on shared Svelte, both platforms), **✅ Phase 6** (AAR re-pin + rebuild + APK), **✅ Manrope slice**, **✅ LS1/2/3**, **✅ §8 ablation**.
- **David's remaining actions:** the emulator visual gate + live sync demo recording + demo video (all human/record-only), then the Sunday eval/robustness/signed-APK/BrainLift work.
- **Audit backlog** (`FUTURE-PLANS.md` `[audit]`) = NON-critical; address opportunistically.
- **Installer/.dmg decision:** Windows installer satisfies the requirement; `.dmg` deferred (macOS-only; unsigned-via-macOS-CI is the documented fallback if ever required). See `docs/WEDNESDAY-DELIVERABLES.md`.
- **Optional infra:** `.claude/cursor-review-notify.ps1` (UserPromptSubmit nudge hook) written, NOT wired into `settings.json` — David relays manually (working well).
- **Lane note:** umbrella docs (incl. this file) are Cursor's lane. Claude should propose STATE/PROGRESS changes via `.claude/cursor-review.md`, not edit umbrella docs directly (avoids stale/duplicate sections + races).

## Mission-control operating notes (for a fresh Cursor chat)
- **Roles (updated 2026-07-01):** Cursor = mission control — orchestration, review, docs, git, design specs. Claude Code (separate app, "Main Orchestration 2" session) = builds engine/Android **and, since today, frontend** (`ts/`+`qt/` — David's call; it keeps the proven subagent-driven-development loop). Cursor avoids concurrent writes in `repos/*` while Claude builds; umbrella docs are Cursor's lane exclusively.
- **Watch Claude's output (Claude → Cursor):** hook writes digests to `.claude/watch.log` (project-level `.claude/settings.json` Stop/SubagentStop hook → `claude-digest.ps1` — survives Claude restarts). Reconnect: background-run `Get-Content .claude/watch.log -Wait -Tail 0` with output-notify on `\]\[(Stop|SubagentStop)\]`. For live detail read the newest transcript: `~/.claude/projects/C--Users-davir-Ultra-Alpha-Speedrun/<newest>.jsonl` (currently `01774af5-…`).
- **Review channel (Cursor → Claude):** `.claude/cursor-review.md` — Cursor writes gate feedback / fix requests here (Pending block at top; move to Resolved when done). Claude reads it at each gate or when David says "read cursor-review.md". Umbrella-only (never pushed to forks). Optional proactive nudge: `.claude/cursor-review-notify.ps1` is a **UserPromptSubmit** hook (written, NOT yet wired into `settings.json`) that surfaces a one-line "new pending review" nudge on David's next prompt if the file changed — non-interrupting (rides on David's own messages, never fires mid-thinking, always exits 0). To enable, add a `UserPromptSubmit` entry to `.claude/settings.json` pointing at that script (do it at a calm moment, not mid-build — editing settings can prompt Claude to re-review).
- **Git:** umbrella docs commit to `spinkicks/speedrun` `main`; pushes to `main` may trigger a smart-mode approval card (expected — approve). The PowerShell `git : ... RemoteException` on push is COSMETIC; check the `->` ref-update line. Code lands on the forks; strategy docs stay in the PRIVATE umbrella only (never push AGENTS.md/strategy to public forks).
- **Key docs:** `docs/PROGRESS.md` (done/left + audit findings), `docs/FUTURE-PLANS.md` (backlog), `docs/plans/2026-07-03-friday-brief.md` (Friday requirements), `docs/RUN-MVP.md` (run steps), `docs/design/*.md` (UI contracts), `docs/artifacts/` (§7a).

## Cadence
Mon=plan · Tue=heavy coding · Wed=both apps MVP (desktop+Android, NO AI) · Fri=AI+sync+3 scores · Sun=evals/ablation/ship.

## Key invariants (also in AGENTS.md)
- Mutations → `Collection::transact(Op::X, …)` returning `OpChanges`; never raw DB writes.
- `proto/anki/*.proto`: append-only field numbers (frozen 2026-07-01 @ `20dd7a2ea`; later additions append-only).
- TDD: failing test first; tests read-only to implementer; `eval/holdout/` off-limits (not yet created — Sunday).
- Ground APIs via Serena/ast-grep/`cargo check` before editing.
- AI strictly after Wednesday (spec rule).
